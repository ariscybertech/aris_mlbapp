name: simple_bet
description: Building an app for MLB.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Calling REST APIs...
  http: ^0.12.0+2

  # State management
  provider: ^3.2.0  

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - assets/images/